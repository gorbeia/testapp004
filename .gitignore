*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing keystores — never commit these (except the shared debug keystore below)
*.keystore
*.jks
*.b64
!keystores/debug.keystore
